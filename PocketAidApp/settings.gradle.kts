pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = "Mental Match"
include(":app")
include(":domain")
include(":data")
